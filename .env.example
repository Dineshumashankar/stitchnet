# StitchNet Production Environment Variables

# --- BACKEND (Set these in Vercel/Railway) ---
# Your Supabase Connection String (from Project Settings -> Database)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-ID].supabase.co:5432/postgres

# Random long string for security
JWT_SECRET=super-secret-random-key-change-this

# Port (Backend usually auto-detects, but good to have)
PORT=5000

# --- FRONTEND (Set these in Netlify/Vercel) ---
# The URL where your Backend is hosted (e.g. https://stitchnet-api.vercel.app/api)
VITE_API_URL=http://localhost:5001/api
